<UserControl x:Class="DockTop.Controls.ToastBar"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="32" Background="#CC1E1E1E" Visibility="Collapsed">
  <UserControl.Resources>
    <Storyboard x:Key="ShowAnim">
      <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" From="0" To="1" Duration="0:0:0.15"/>
    </Storyboard>
    <Storyboard x:Key="HideAnim">
      <DoubleAnimation Storyboard.TargetProperty="(UIElement.Opacity)" From="1" To="0" Duration="0:0:0.25"/>
    </Storyboard>
  </UserControl.Resources>
  <Grid>
    <TextBlock x:Name="Msg" Foreground="White" VerticalAlignment="Center" Margin="10,0" TextWrapping="NoWrap"/>
  </Grid>
</UserControl>
